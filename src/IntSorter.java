/**
 * An interface which describes what a sorting algorithm that sorts integers should be able to do.
 * 
 * @author Björn Tegelund
 * @version 2011.02.23
 */
public interface IntSorter {
    /**
     * Sorts the array into ascending numerical order.
     */
    void sort(int[] v);
}